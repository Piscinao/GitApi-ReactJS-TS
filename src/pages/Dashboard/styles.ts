import styled from 'styled-components';

// Template literals -- template string
// css do styled vale tanto para o reacjs e para o native
export const Title = styled.h1`
  font-size: 48px;
  color: #3A3A3A;
  `;
